#!/usr/bin/python

if__name__==__"main__":
    """Print the hidden alphabet order"""
    import hidden_4

    names = dir(hidden_4)
    for name in names:
        if name[:2]!="__"
            print(name)
